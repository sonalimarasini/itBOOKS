Please open index.html from localhost.

The replica webpages can be nagivated from index.html
The files with v2 at the end of the filename is the replica webpages. 
These html files are the same as the original html files. 
The only difference is that v2 html/php files have style1.css instead of style.css